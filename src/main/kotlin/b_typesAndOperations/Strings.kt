package b_typesAndOperations

fun main() {
    /**
     * How computers represent Strings
     */

    /*
    Character set : When you map a character to a number.
    We do that because the CPU knows only numbers.
     */

    /**
     * Unicode
     */

    /*
    Unicode is a character set standard.
    It defines the character set mapping that all computers use today.
    Each character is associated with a number.
    This number is called `code point`.
    Unicode maps character from different languages also.
    Emojis are also mapped to `code points`, they are also characters ☺️.
     */
}